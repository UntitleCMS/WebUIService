import { LoveButtonDirective } from './love-button.directive';

describe('LoveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
