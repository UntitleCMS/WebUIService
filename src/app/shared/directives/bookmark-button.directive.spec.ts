import { BookmarkButtonDirective } from './bookmark-button.directive';

describe('BookmarkButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BookmarkButtonDirective();
    expect(directive).toBeTruthy();
  });
});
